# TruthBot Zero — Homepage Patched Kit

This kit contains:
- A **patched index.html** with the correct `<picture>` block for the logo.
- The canonical logo saved as `assets/truthbot_zero_logo.png`.

## How to install
1. Unzip into your local repo folder:
   `~/Documents/PovertyPimpsVault/papiricanpi.github.io/`
   (Allow it to overwrite `index.html` and merge the `assets/` folder).

2. Commit & push:
   ```bash
   cd ~/Documents/PovertyPimpsVault/papiricanpi.github.io
   git add index.html assets/truthbot_zero_logo.png
   git commit -m "Homepage patched with TruthBot Zero logo"
   git push
   ```

3. Hard refresh your site (Cmd+Shift+R).  
4. Test:
   https://papiricanpi.github.io/assets/truthbot_zero_logo.png